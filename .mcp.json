{
  "mcpServers": {
    "context7": {
      "command": "cmd.exe",
      "args": ["/c", "npx", "-y", "@upstash/context7-mcp"],
      "description": "최신 라이브러리 문서 (NetworkX, FastAPI, sentence-transformers 등)"
    }
  }
}
